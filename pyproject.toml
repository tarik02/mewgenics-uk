[project]
name = "mewgenics-uk"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "openai>=2.20.0",
]

[dependency-groups]
build = [
    "pyinstaller>=6.0",
]
dev = [
    "pyinstaller>=6.18.0",
]
